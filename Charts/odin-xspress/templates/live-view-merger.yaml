{{- $enabled := eq .Values.global.enabled false | ternary false true -}}
kind: StatefulSet
apiVersion: apps/v1
metadata:
  name: {{ .Values.detectorName }}-live-view-merger
  labels:
    enabled: {{ $enabled | quote }}
spec:
  replicas: {{ $enabled | ternary 1 0 }}
  selector:
    matchLabels:
      app: {{ .Values.detectorName }}-live-view-merger
  template:
    metadata:
      labels:
        app: {{ .Values.detectorName }}-live-view-merger
    spec:
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: {{ .Values.detectorName }}-odin-config-volume
          configMap:
            name: {{ .Values.detectorName }}-odin-config
            defaultMode: 0755
        {{- with .Values.dataVolume }}
        - name: {{ $.Values.detectorName }}-data
          hostPath:
            path: {{ .hostPath }}
            type: Directory
        {{- end }}
      containers:
        - name: {{ .Values.detectorName }}-live-view-merger
          image: {{ .Values.image }}
          command:
            - /odin/deployment/live-view-merger.sh
          ports:
            - containerPort: 15510
          volumeMounts:
            - name: {{ .Values.detectorName }}-odin-config-volume
              mountPath: /odin/deployment
            {{- with .Values.dataVolume }}
            - name: {{ $.Values.detectorName }}-data
              mountPath: {{ .hostPath }}
              mountPropagation: HostToContainer
            {{- end }}
          {{- with .Values.metaWriterResources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with $.Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: OPENSSL_FORCE_FIPS_MODE
              value: "0"
      terminationGracePeriodSeconds: 3
---
kind: Service
apiVersion: v1
metadata:
  name: {{ .Values.detectorName }}-live-view-merger
spec:
  selector:
    app: {{ .Values.detectorName }}-live-view-merger
  ports:
    - name: {{ .Values.detectorName }}-live-view-merger-ctrl
      protocol: TCP
      port: 15510
